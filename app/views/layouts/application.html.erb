<!DOCTYPE html>
<html>
<head>
  <title>Appointment</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <script src='https://kit.fontawesome.com/a076d05399.js' crossorigin='anonymous'></script>
  <style>
    table, th, td {
      border:1px solid black;
    }

    body {
      margin: 0;
      font-family: Arial, Helvetica, sans-serif;
    }
    .topnav {
      overflow: hidden;
      background-color: #333;
    }

    .topnav a {
      float: left;
      color: #f2f2f2;
      text-align: center;
      padding: 14px 16px;
      text-decoration: none;
      font-size: 17px;
    }

    .topnav a:hover {
      background-color: #ddd;
      color: black;
    }

    .topnav a {
      background-color: #04AA6D;
      color: white;
    }
    .topnav-right {
      float: right;
    }
    .topnav a:hover {background-color: #f1f1f1}

    .topnav:hover .topnav-right {
      display: block;
    }
    .navbar {
      width: 100%;
      background-color: #555;
      overflow: auto;
    }

    .navbar a {
      float: left;
      padding: 12px;
      color: white;
      text-decoration: none;
      font-size: 17px;
    }

    .navbar a:hover {
      background-color: #000;
    }
  </style>
</head>
<body>
  <% if user_signed_in? %>
  Logged in as <strong><%= current_user.email %></strong>
  <!-- <div class="topnav">
    <a class="active" href="#"><i class="fa fa-fw fa-home"></i> Home</a> 
    <a href="<%#= doctor_index_path(@doctor) %>"><i class="fas fa-heartbeat"style='font-size:36px;color:red'></i> Doctor</a> 
    <a href="<%#= patient_appointments_path(@patientAppointment) %>"><i class="far fa-clock"style='font-size:36px;color:red;'></i>Appointments</a> 
  </div> -->

  <div class="topnav">
  <%= link_to "Appointments",patient_appointments_path %>
  <%= link_to "Doctors", doctor_index_path %>
  <%= link_to "Reports",report_index_path%>
    <%#= link_to "Sign out", destroy_user_session_path, :method => :delete%>
  
  <div class="topnav-right">
  <!-- <a href="#">Profile</a> -->
  <%= link_to "Sign out", destroy_user_session_path, :method => :delete%>
  
  </div>
  </div>
  <% end %>

  <%= yield %>

</body>
</html>
